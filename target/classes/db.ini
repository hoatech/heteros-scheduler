\\ Specifies the number of slots for a single time window -> the window defines the time range over which the cpu-load will be computed
time.window.slot.count=12
\\ Specifies the length (in seconds) of a single slot of a time window -> so we have 12 5 second slots, which will give a window of 1 minute. whatever that means...
time.window.slot.length=5
